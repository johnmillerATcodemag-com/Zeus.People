# azure.yaml - Azure Developer CLI configuration for Zeus.People
name: academic-staging
metadata:
  template: academic-staging@0.0.1-beta
infra:
  provider: bicep
  path: infra
  module: main
services:
  api:
    project: src/API
    host: appservice
    language: dotnet
    dist: publish
